package test.scraper

class ScraperTest extends GroovyTestCase {
    void testCache() {
        // C:\Users\---\AppData\Local\Temp\temp6795181128588238066.scrap
        // File temp = File.createTempFile("temp",".scrap");
        // println temp.getAbsolutePath()
        // Create temps folder.
        // download known file / that should not move(a file from the repo?)
        // Validate that the content equals what is expected.
    }
}
